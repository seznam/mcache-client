language: cpp
compiler:
    - clang
    - gcc
addons:
    apt:
        sources:
            - boost-latest
        packages:
            - libboost1.55-all-dev
            - libtbb2
install:
    - autoreconf --install --force
script:
    - make check
